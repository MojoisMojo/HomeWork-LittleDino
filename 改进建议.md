# HomeWork-LittleDino 改进审计

## 审计范围

- 本文基于当前工作树的静态代码审查编写。
- 未运行游戏、未执行自动化测试。已使用新的 MSYS2 MINGW64 SDL 开发环境通过 CMake 完成编译和链接；这只验证构建链路，不验证运行时表现和发布包兼容性。
- 结论分为“确定缺陷”“低风险维护项”和“需要项目决策的建议”，避免将推测写成 Bug。

## 优先修复的确定缺陷

### P0：障碍物链表存在释放后访问

**结论：确定缺陷。**

- 在 [src/Obs.c:63](src/Obs.c#L63) 的 `MoveObs` 循环中，节点可能经 [src/Obs.c:31](src/Obs.c#L31) 的 `DeleteObs` 释放；循环的递增表达式随后仍读取 `pObs->next`。
- 在 [src/Obs.c:50](src/Obs.c#L50) 的 `ResetObs` 循环中，`free(pObs)` 后递增表达式继续读取 `pObs->last`。
- 这两处都是释放后解引用，属于未定义行为。前者会在障碍物离屏删除时触发，后者会在至少玩过一局后重新开始时触发。

**建议：** 在释放前保存 `next` 或 `last`，并改成由保存指针推进的循环；重置逻辑可复用 `DeleteObs`，避免同时维护链表指针和计数器。

**修复后验证：** 连续开始/退出多局，并让不同类型障碍物多次离屏；使用 AddressSanitizer 或同类内存检查工具确认没有释放后访问。

### P1：爬行状态会提前结束整条碰撞遍历

**结论：确定实现错误，实际游戏影响取决于障碍物重叠时序。**

- [src/DinoGame.c:313](src/DinoGame.c#L313) 遍历全部障碍物。
- 遇到飞鸟且角色爬行时，[src/DinoGame.c:324](src/DinoGame.c#L324) 直接 `return false`，后续地面障碍物不再检查。
- 应跳过当前飞鸟并继续遍历，而不是把“当前飞鸟不会撞到”解释为“没有任何碰撞”。

**建议：** 将该分支改为继续下一轮循环，或让 `SKY` 分支只结束当前 `switch` 分支。

**修复后验证：** 在角色爬行、飞鸟与地面障碍物同时接近时，确认地面障碍物仍会触发失败。

### P1：资源和存档失败路径会导致空指针访问

**结论：确定鲁棒性缺陷。**

- `SDL_Init`、`TTF_Init` 以及各资源加载调用未检查返回值，例如 [src/DinoGame.c:393](src/DinoGame.c#L393) 和 [src/DinoGame.c:415](src/DinoGame.c#L415)。首个背景资源加载失败后，[src/DinoGame.c:419](src/DinoGame.c#L419) 会直接访问空 `SDL_Surface`。
- [src/DinoGame.c:494](src/DinoGame.c#L494) 以 `r+` 打开最高分文件，却在 [src/DinoGame.c:495](src/DinoGame.c#L495) 无条件读取。文件缺失、只读或工作目录不正确时会崩溃。
- [src/DinoGame.c:408](src/DinoGame.c#L408) 用无宽度限制的 `%s` 读取到 20 字节缓冲区，异常 `Win.txt` 内容可越界写入。

**建议：** 建立统一的初始化/加载错误路径；读取最高分失败时采用默认值；用栈数组和 `%19s` 读取 `Win.txt`；写入最高分前也检查文件句柄。

**修复后验证：** 分别测试缺少图片、缺少字体、缺少/只读最高分文件和异常 `Win.txt` 的启动结果。

### P2：随机数播种语句被错误地包含在宏定义中

**结论：确定缺陷。**

- [src/Base.h:16](src/Base.h#L16) 的 `MySwap` 宏每行都使用续行符，导致 [src/Base.h:20](src/Base.h#L20) 的 `srand(time(NULL));` 成为宏体的一部分。
- 当前仓库没有 `MySwap` 调用，因此 `srand` 不会执行，`rand()` 生成的云和障碍物序列在每次启动时相同。
- 该宏还引用未定义的 `t`，未来一旦使用会产生编译错误。

**建议：** 删除或修正 `MySwap`；在 `BEGIN` 中单独调用一次 `srand((unsigned)time(NULL))`。

### P2：非游戏界面空闲时忙轮询

**结论：确定性能问题。**

- 主菜单、失败界面、暂停界面和帮助界面都以外层循环配合 `SDL_PollEvent` 轮询，例如 [src/DinoGame.c:16](src/DinoGame.c#L16)、[src/DinoGame.c:189](src/DinoGame.c#L189)、[src/DinoGame.c:236](src/DinoGame.c#L236)、[src/Help.c:28](src/Help.c#L28)。
- 队列为空时，外层循环不会等待或延迟，空闲界面会持续占用 CPU。

**建议：** 静态界面使用 `SDL_WaitEvent`，或在无事件时加入短暂延迟；游戏主循环现有的 [src/DinoGame.c:93](src/DinoGame.c#L93) 延迟不需要按此处理。

## 复核后降级或改写的建议

### 发布文件与构建配置

**结论：不是已证实的功能 Bug，属于项目发布策略。**

仓库跟踪了 `src/DinoGame.exe` 以及三份 SDL DLL，但没有构建脚本或 `.gitignore`。这会降低源码构建的可复现性，也可能使“从哪个目录启动程序”和 DLL 搜索路径变得不明确；但保留二进制也可能是作者有意提供的免构建运行包。

**建议：** 明确选择一种模式：

- 源码仓库：加入 CMake 或 Makefile、说明依赖安装步骤，并忽略生成的 EXE/DLL。
- 发布仓库：保留运行包，但将 EXE、DLL、`image/`、`data/` 和启动方式放入同一发行目录，并在 README 中说明。

实际打包是否可直接运行需要后续在干净 Windows 环境中验证，本文不将其标记为当前缺陷。

### `BGD.h` 的 include guard

**结论：确定的低风险维护缺陷。**

[src/BGD.h:1](src/BGD.h#L1) 检查 `BGD_H`，但 [src/BGD.h:2](src/BGD.h#L2) 定义的是 `BND_H`。当前工程只有一个 `.c` 翻译单元，未见由此造成的现有构建失败；但一旦该头文件被重复包含，可能出现重复定义。

**建议：** 将第二行改为 `#define BGD_H`，并将其与后续模块化改造一起处理。

### 头文件中的全局变量和函数

**结论：本次形式拆分已完成。**

原始实现通过单个 `src/DinoGame.c` 包含所有头文件来工作，因此当时不会立即产生多重定义链接错误。本次已将全局变量定义和函数体移入对应模块 `.c` 文件，头文件仅保留类型、声明和 `extern`；`DinoGame.c` 仍是唯一的应用编排入口。这是边界整理，不包含 P0/P1 缺陷修复。

### 每帧创建文字纹理

**结论：可优化项，尚未证明为性能瓶颈。**

[src/WordsAndFont.c:11](src/WordsAndFont.c#L11) 和 [src/WordsAndFont.c:23](src/WordsAndFont.c#L23) 会在每帧创建并销毁文字表面和纹理，但对应资源随后已正确释放，因此不是泄漏。游戏较简单时该开销可能可接受。

**建议：** 仅在性能分析显示其占比明显时，缓存不变文本，或仅在分数显示内容改变时重新生成纹理。

## 推荐实施顺序

1. 修复 `Obs.c` 的两处释放后访问，并补充连续重开和障碍物离屏测试。
2. 修复碰撞遍历的提前返回。
3. 补全资源、字体和存档的失败处理，再处理随机数播种。
4. 消除菜单类界面的忙轮询。
5. 依据项目发布目标决定构建脚本、二进制文件和目录布局；头文件/实现的形式拆分已完成，之后再考虑渲染缓存等性能重构。

## 未执行的验证

本文没有运行游戏或自动化测试。CMake 已使用新的 MSYS2 MINGW64 SDL 开发环境成功编译并链接全部源文件，但这不验证运行时资源加载、碰撞缺失的实际复现频率或文字渲染的性能收益。以上 P0/P1/P2 结论仍来自明确的控制流和内存生命周期分析。
