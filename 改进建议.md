# HomeWork-LittleDino 改进审计

## 审计范围与当前状态

- [KNOWN, HIGH] 本文依据当前模块化后的 C 源码、CMake 配置和已生成的 Debug/Release 可执行文件更新。
- [KNOWN, HIGH] 当前工程已拆为 11 个 C 编译单元：`DinoGame.c` 负责应用入口，其余 10 个模块分别拥有实现与全局定义；头文件保留类型、函数声明和 `extern` 声明。
- [KNOWN, HIGH] CMake 已显式列出全部源文件并复制 `image`、`font`、`data` 资源目录。
- [KNOWN, HIGH] 已完成 Debug 与 Release 的构建验证：Debug 可执行文件为 Windows CUI，保留控制台输出；Release 可执行文件为 Windows GUI，只显示游戏窗口。
- [KNOWN, HIGH] 本次未启动游戏，也未执行自动化测试；构建成功不能证明资源缺失、碰撞边界或链表生命周期在运行时正确。

## 本次已修复

### P0: 障碍物链表释放后访问

[KNOWN, HIGH] 修复前，[ResetObs](src/Obs.c#L50) 在 `free(pObs)` 后由 `for` 的递增表达式读取 `pObs->last`；[MoveObs](src/Obs.c#L65) 在 [DeleteObs](src/Obs.c#L31) 释放当前节点后，由 `for` 的递增表达式读取 `pObs->next`。

[KNOWN, HIGH] 当前实现均在释放前保存相邻节点指针：`ResetObs` 保存 `last`，`MoveObs` 保存 `next`；遍历次数与原逻辑相同，`DeleteObs` 仍负责断链和更新 `cntObs`。

[KNOWN, HIGH] GCC 16.1 的 `-fanalyzer` 在修复前对两条路径均报告 `CWE-416 use-after-free`；修复后重新分析，相关诊断已消失。Debug 与 Release 均已重新编译通过。

**仍需验证：** 尚未进行游戏运行或 AddressSanitizer 测试；后续应连续开始、结束并重新开始多局，并让地面和天空障碍反复离屏。

### P1: 爬行时的飞鸟不应中止整条碰撞遍历

[KNOWN, HIGH] 修复前，[CheckCollisions](src/DinoGame.c#L556) 在角色爬行且当前障碍为天空障碍时直接返回 `false`，错误地把“当前飞鸟不碰撞”解释为“后续障碍也不碰撞”。

[KNOWN, HIGH] 当前实现仅在非爬行状态检查飞鸟碰撞；爬行时会跳过当前飞鸟，并继续检查链表中的后续障碍物。

**仍需验证：** 尚未启动游戏。后续应构造角色爬行、飞鸟和地面障碍在同一时间窗口接近的场景，确认地面障碍仍可触发失败。

### P1: 启动、资源与存档失败路径

[KNOWN, HIGH] 修复前，[BEGIN](src/DinoGame.c#L633) 未检查 SDL、SDL_image、SDL_ttf、窗口和渲染器初始化结果；[LOAD](src/DinoGame.c#L672) 失败后仍可能使用空的表面、纹理或字体指针。

[KNOWN, HIGH] 当前实现统一检查初始化、[IMG_Load](src/DinoGame.c#L28)、纹理创建和字体加载结果；失败时报告错误，释放已创建资源，并阻止进入主循环。

[KNOWN, HIGH] `Win.txt` 使用固定数组和 `%19s` 读取；缺失或异常的最高分文件回退到 `0`，退出保存失败也只记录错误，不再解引用空文件句柄。

[KNOWN, HIGH] 菜单、帮助页和分数/等级文字的临时表面与纹理也检查失败结果；`ReleaseResources` 负责清理部分初始化留下的窗口、渲染器、表面、纹理和字体。

[KNOWN, HIGH] Debug 与 Release 均已重新编译通过；GCC 静态分析未发现本次失败路径处理引入的新空指针诊断。

**仍需验证：** 尚未启动游戏。后续应分别测试缺图、缺字体、缺存档、只读存档和异常 `Win.txt` 的启动及退出路径。

### P2: 随机数播种与 `MySwap` 宏（已修复）

[KNOWN, HIGH] 修复前，`srand(time(NULL))` 被续行符并入未使用的 `MySwap` 宏，因此不会执行；同一宏还引用未定义标识符 `t`。

[KNOWN, HIGH] 当前 [BEGIN](src/DinoGame.c#L679) 在渲染器创建成功后显式执行一次 `srand((unsigned)time(NULL))`，之后才进入会调用 `rand` 的资源与游戏逻辑。

[KNOWN, HIGH] 当前 [MySwap](src/Base.h#L16) 是单语句 `do { ... } while (0)` 宏，使用保存的临时变量完成交换；参数应为可赋值的 `int` 左值。

**仍需验证：** 尚未启动游戏。后续可在不同秒启动多次，确认云、月亮和障碍物序列不再固定。

### P2: 静态界面事件等待（已修复）

[KNOWN, HIGH] 修复前，主菜单、暂停、失败和帮助界面的外层循环持续调用 `SDL_PollEvent`；事件队列为空时会立即重复循环。

[KNOWN, HIGH] 当前四个静态界面使用 `SDL_WaitEvent` 等待输入；没有事件时主线程休眠并让出 CPU，收到事件后再继续处理。游戏主循环 `InPlay` 仍保留 `SDL_PollEvent` 与 `SDL_Delay` 的帧率控制。

**仍需验证：** 尚未启动游戏。后续应确认菜单、帮助、暂停和失败界面在等待期间仍能正常响应按键、鼠标和关闭窗口事件。

### P2: 跳跃中按住 DOWN 的状态同步（已修复）

[KNOWN, HIGH] 修复前，跳跃中的 `DOWN` 只在收到一次 `SDL_KEYDOWN` 时把 `JumpSpeed` 设为快速下落值；落地逻辑随后无条件切回 `StandL`，因此持续按住时可能先显示一帧直立姿态，再由后续按键事件进入爬行。

[KNOWN, HIGH] 当前角色移动逻辑在 `DinoCharecter.c` 读取 `SDL_GetKeyboardState` 判断下键是否按住；空中收到 `S` 或方向下键事件时直接将 `JumpSpeed` 设为 `3.0`，落地时根据当前按键状态直接选择 `CrawlR`，释放两个下落键后才恢复站立。`MainEvent` 仍只负责逐个接收 SDL 事件，不是多键输入的容量限制。

**仍需验证：** 尚未启动游戏。后续应分别测试短按、持续按住、落地前松开，以及同时按住 `S` 和方向下键的情况。

## 尚未修复的缺陷

### P2: CMake 输出不是独立可分发包

[KNOWN, HIGH] [CMakeLists.txt](CMakeLists.txt#L40) 只复制资源目录；Debug 和 Release 输出目录都没有 SDL DLL。

[INFERRED, HIGH] 两个可执行文件都导入 `SDL2.dll`、`SDL2_image.dll` 和 `SDL2_ttf.dll`；在没有其他 DLL 搜索路径的干净环境中，它们将无法启动。

**建议：** 在 Windows 构建后复制目标依赖 DLL，或添加明确的 `install`/打包目标；在干净环境中做一次启动冒烟测试。

## 低风险维护项

### P3: `BGD.h` 的 include guard 名称不一致（已修复）

[KNOWN, HIGH] [BGD.h](src/BGD.h#L1) 现在检查并定义同一个 `BGD_H` 宏，重复包含时 include guard 生效。

[KNOWN, HIGH] Debug 与 Release 均已重新编译通过，GCC 的 `-Wheader-guard` 诊断已消失。

### P3: 每帧创建和销毁文字纹理

[KNOWN, HIGH] [WordsAndFont.c](src/WordsAndFont.c#L11) 与 [WordsAndFont.c](src/WordsAndFont.c#L23) 每次绘制分数和等级都创建、使用并销毁表面与纹理。

[INFERRED, MED] 对这个小型项目，它可能不是当前性能瓶颈，但分数或等级不变时仍反复分配资源。

**建议：** 只在显示文本变化时重建纹理；是否实施应以实际性能测量为准。

## 已完成或已改写的旧结论

- [KNOWN, HIGH] “实现全部塞在头文件中”不再是当前问题：函数体和全局定义已经迁移到对应 `.c` 文件，拆分本身没有修改上述功能缺陷。
- [KNOWN, HIGH] “没有 CMake / `.gitignore`”不再成立：CMake 已覆盖全部编译单元，`.gitignore` 已忽略常用构建目录及其产物。
- [KNOWN, HIGH] “所有构建都会弹出控制台”不再成立：Debug 保留控制台，Release 系列使用 GUI 子系统；入口由 `main` 与 [WinMain](src/DinoGame.c#L243) 共用启动逻辑。
- [KNOWN, HIGH] 未找到常规命名的测试或许可证文件；README 仍说明恐龙图片来自 Chrome，资源再发布权利尚未明确。

## 推荐实施顺序

1. 补齐 SDL DLL 打包和干净环境启动验证。
2. 处理文字纹理缓存及全局状态收拢等维护性改进。
