# HomeWork-LittleDino 改进审计

## 审计范围与当前状态

- [KNOWN, HIGH] 本文依据当前模块化后的 C 源码、CMake 配置和已生成的 Debug/Release 可执行文件更新。
- [KNOWN, HIGH] 当前工程已拆为 11 个 C 编译单元：`DinoGame.c` 负责应用入口，其余 10 个模块分别拥有实现与全局定义；头文件保留类型、函数声明和 `extern` 声明。
- [KNOWN, HIGH] CMake 已显式列出全部源文件并复制 `image`、`font`、`data` 资源目录。
- [KNOWN, HIGH] 已完成 Debug 与 Release 的构建验证：Debug 可执行文件为 Windows CUI，保留控制台输出；Release 可执行文件为 Windows GUI，只显示游戏窗口。
- [KNOWN, HIGH] 本次未启动游戏，也未执行自动化测试；构建成功不能证明资源缺失、碰撞边界或链表生命周期在运行时正确。

## 已证实的缺陷

### P0: 障碍物链表存在释放后访问

[KNOWN, HIGH] [ResetObs](src/Obs.c#L50) 在 `free(pObs)` 后由 `for` 的递增表达式读取 `pObs->last`。

[KNOWN, HIGH] [MoveObs](src/Obs.c#L63) 在 [DeleteObs](src/Obs.c#L31) 释放当前节点后，由 `for` 的递增表达式读取 `pObs->next`。

[KNOWN, HIGH] 两处均是 C 语言未定义行为，不能因为某次运行未崩溃而视为安全。

**建议：** 在释放前保存 `next` 或 `last`，让遍历使用保存的指针推进。`ResetObs` 应复用链表删除规则，避免独立维护链表指针与计数器。

**修复后验证：** 连续开始、结束并重新开始多局；让地面和天空障碍反复离屏；使用 AddressSanitizer 或同类内存检查工具。

### P1: 爬行时会提前结束整条碰撞遍历

[KNOWN, HIGH] [CheckCollisions](src/DinoGame.c#L335) 遇到天空障碍且角色爬行时，在 [DinoGame.c:346](src/DinoGame.c#L346) 直接返回 `false`。

[KNOWN, HIGH] 该返回会跳过链表中后续障碍，包括可能已经重叠的地面障碍。

**建议：** 跳过当前天空障碍并继续遍历，而不是把“当前飞鸟不碰撞”解释为“所有障碍均未碰撞”。

**修复后验证：** 构造角色爬行、飞鸟和地面障碍在同一时间窗口接近的场景，确认地面障碍仍可触发失败。

### P1: 初始化、资源与存档失败路径不安全

[KNOWN, HIGH] [BEGIN](src/DinoGame.c#L414) 未检查 `SDL_Init`、`TTF_Init`、窗口和渲染器创建结果。

[KNOWN, HIGH] [LOAD](src/DinoGame.c#L427) 中多次使用 `IMG_Load`、`TTF_OpenFont` 和纹理创建结果，但失败后会继续解引用相关指针。

[KNOWN, HIGH] [DinoGame.c:430](src/DinoGame.c#L430) 用无宽度限制的 `%s` 读取 20 字节缓冲区，异常的 `Win.txt` 内容可越界写入。

[KNOWN, HIGH] [DinoGame.c:516](src/DinoGame.c#L516) 和 [DinoGame.c:517](src/DinoGame.c#L517) 未检查最高分文件句柄便读取；退出时的写入句柄也未检查。

**建议：** 建立统一的初始化失败清理路径；资源、窗口、渲染器、字体和文件操作均检查返回值；使用固定数组与 `%19s`；最高分读取失败时使用默认值。

**修复后验证：** 分别测试缺图、缺字体、缺存档、只读存档和异常 `Win.txt` 的启动及退出路径。

### P2: 随机数没有实际播种，`MySwap` 宏本身也不可用

[KNOWN, HIGH] [Base.h](src/Base.h#L16) 中 `srand(time(NULL))` 被续行符并入 `MySwap` 宏；仓库内没有该宏的调用，因此播种不会执行。

[KNOWN, HIGH] 同一宏引用未定义标识符 `t`，未来一旦使用会导致编译失败或错误行为。

**建议：** 删除或修正 `MySwap`，并在 [BEGIN](src/DinoGame.c#L414) 中显式执行一次 `srand((unsigned)time(NULL))`。

### P2: 非游戏界面处于忙轮询状态

[KNOWN, HIGH] 主菜单、暂停、失败和帮助界面的外层循环持续调用 `SDL_PollEvent`；帮助页示例见 [Help.c](src/Help.c#L28)。

[INFERRED, HIGH] 事件队列为空时，这些界面会持续占用一个 CPU 核心的可用时间片，静态界面没有必要如此轮询。

**建议：** 静态界面使用 `SDL_WaitEvent`，或在无事件时加入短暂延迟；游戏主循环可保留自己的帧率控制。

### P2: CMake 输出不是独立可分发包

[KNOWN, HIGH] [CMakeLists.txt](CMakeLists.txt#L40) 只复制资源目录；Debug 和 Release 输出目录都没有 SDL DLL。

[INFERRED, HIGH] 两个可执行文件都导入 `SDL2.dll`、`SDL2_image.dll` 和 `SDL2_ttf.dll`；在没有其他 DLL 搜索路径的干净环境中，它们将无法启动。

**建议：** 在 Windows 构建后复制目标依赖 DLL，或添加明确的 `install`/打包目标；在干净环境中做一次启动冒烟测试。

## 低风险维护项

### P3: `BGD.h` 的 include guard 名称不一致

[KNOWN, HIGH] [BGD.h](src/BGD.h#L1) 检查 `BGD_H`，第二行却定义 `BND_H`，因此该 guard 实际不生效。

[KNOWN, HIGH] 当前所有源文件仍可编译，说明现有包含路径没有触发重复定义；但任何重复包含都可能暴露该问题。

**建议：** 将第二行改为 `#define BGD_H`。

### P3: 每帧创建和销毁文字纹理

[KNOWN, HIGH] [WordsAndFont.c](src/WordsAndFont.c#L11) 与 [WordsAndFont.c](src/WordsAndFont.c#L23) 每次绘制分数和等级都创建、使用并销毁表面与纹理。

[INFERRED, MED] 对这个小型项目，它可能不是当前性能瓶颈，但分数或等级不变时仍反复分配资源。

**建议：** 只在显示文本变化时重建纹理；是否实施应以实际性能测量为准。

## 已完成或已改写的旧结论

- [KNOWN, HIGH] “实现全部塞在头文件中”不再是当前问题：函数体和全局定义已经迁移到对应 `.c` 文件，拆分本身没有修改上述功能缺陷。
- [KNOWN, HIGH] “没有 CMake / `.gitignore`”不再成立：CMake 已覆盖全部编译单元，`.gitignore` 已忽略常用构建目录及其产物。
- [KNOWN, HIGH] “所有构建都会弹出控制台”不再成立：Debug 保留控制台，Release 系列使用 GUI 子系统；入口由 `main` 与 [WinMain](src/DinoGame.c#L22) 共用启动逻辑。
- [KNOWN, HIGH] 未找到常规命名的测试或许可证文件；README 仍说明恐龙图片来自 Chrome，资源再发布权利尚未明确。

## 推荐实施顺序

1. 修复 `Obs.c` 的两处释放后访问，并加入重复开局、障碍离屏的内存安全验证。
2. 修复碰撞遍历的提前返回。
3. 补齐初始化、资源、文件读写的失败处理，并修复随机数播种。
4. 为静态界面消除忙轮询。
5. 补齐 SDL DLL 打包和干净环境启动验证。
6. 处理 include guard、文字纹理缓存及全局状态收拢等维护性改进。
